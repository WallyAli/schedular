
  <div>
    <%= f.label :first_name %>
    <%= f.text_field :first_name %>
  </div>
    <div>
    <%= f.label :last_name %>
    <%= f.text_field :last_name %>
  </div>

  <%= f.fields_for :days do |builder| %>
    <%= render 'day_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Add Days", f, :days %>
