
<h3>Schedule</h3>
<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr class="info">
        <th>First name</th> 
        <th>Last name</th> 
        <th>Time-In</th>
        <th>Sign-In</th>
        <th>Time-Out</th>
        <th>Sign-Out</th>
      </tr>
    </thead>

    <tbody>
      <h4><%= @schedule.name %> - <%= Date.today %></h4>
      <% @schedule.children.each do |child| %>
        <tr>
          <td ><%= link_to child.first_name, child %></td> 
          <td ><%= link_to child.last_name, child %></td> 
          <td ><%= child.time_in %></td> 
          <td ><%= child.time_out %></td> 
          <td ></td>
          <td ></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>